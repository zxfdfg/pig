<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pig4cloud.pig.product.mapper.CategoryMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="baseResultMap" type="com.pig4cloud.pig.product.api.entity.ProductCategory">
		<id column="id" property="id"/>
		<result column="parent_id" property="parentId"/>
		<result column="name" property="name"/>
		<result column="icon" property="icon"/>
		<result column="description" property="description"/>
		<result column="sort" property="sort"/>
		<result column="level" property="level"/>
		<result column="create_time" property="createTime"/>
		<result column="update_time" property="updateTime"/>
		<result column="del_flag" property="delFlag"/>
	</resultMap>

	<!-- 查询分类树 -->
	<select id="listCategoryTree" resultMap="baseResultMap">
		SELECT * FROM product_category
		WHERE del_flag = 0
		ORDER BY sort DESC, create_time ASC
	</select>

	<!-- 查询顶级分类 -->
	<select id="listTopCategories" resultMap="baseResultMap">
		SELECT * FROM product_category
		WHERE parent_id = 0
		AND del_flag = 0
		ORDER BY sort DESC, create_time ASC
	</select>

	<!-- 查询子分类 -->
	<select id="listChildCategories" resultMap="baseResultMap">
		SELECT * FROM product_category
		WHERE parent_id = #{parentId}
		AND del_flag = 0
		ORDER BY sort DESC, create_time ASC
	</select>

	<!-- 递归查询所有子分类ID -->
	<select id="listAllChildCategoryIds" resultType="java.lang.Long">
		WITH RECURSIVE category_tree AS (
			SELECT id, parent_id FROM product_category WHERE id = #{categoryId} AND del_flag = 0
			UNION ALL
			SELECT c.id, c.parent_id FROM product_category c
			INNER JOIN category_tree ct ON c.parent_id = ct.id
			WHERE c.del_flag = 0
		)
		SELECT id FROM category_tree
	</select>

	<!-- 查询分类详情 -->
	<select id="getCategoryById" resultMap="baseResultMap">
		SELECT * FROM product_category
		WHERE id = #{id} AND del_flag = 0
	</select>

	<!-- 统计子分类数量 -->
	<select id="countChildCategories" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM product_category
		WHERE parent_id = #{categoryId}
		AND del_flag = 0
	</select>

</mapper>
